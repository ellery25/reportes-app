// TypeScript stub — Metro uses SignatureField.native.tsx on iOS/Android and SignatureField.web.tsx on web.
// This file is never bundled at runtime.
import React from 'react';

interface Props {
  value?: string;
  onChange: (base64: string) => void;
  label?: string;
}

export function SignatureField(_props: Props) {
  return null as unknown as React.ReactElement;
}
